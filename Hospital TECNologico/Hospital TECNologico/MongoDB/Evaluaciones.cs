using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using MongoDB.Bson;
using MongoDB.Driver;

namespace Hospital_TECNologico.MongoDB
{
    public class Evaluaciones
    {
        private const string ConnectionString = "mongodb+srv://<username>:<password>@<cluster-address>/test?w=majority";
        //var client = new MongoClient(ConnectionString);
        //private var database = client.GetDatabase("test");
    }
}
